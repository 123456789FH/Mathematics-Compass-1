# تغيير الشعار

استبدلي الملف:

```text
assets/images/logo.jpg
```

بشعار جديد يحمل الاسم نفسه. ثم أعيدي إنشاء أيقونتي التطبيق `icon-192.png` و`icon-512.png` أو اتركيهما كما هما إذا لم يتغير الشعار.

جميع الواجهات تستدعي الشعار من مسار واحد، لذلك لا يلزم تعديل `index.html`.
